{
  "name": "sehpathi-ai",
  "version": "2.1.0",
  "description": "Teacher-centric analytics platform focused on SDG 4",
  "scripts": {
    "dev": "concurrently --kill-others --prefix \"{name}\" --names \"backend,frontend\" --prefix-colors \"yellow,cyan\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && uvicorn main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "setup": "npm run install:all && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
